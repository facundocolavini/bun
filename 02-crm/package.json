{
  "name": "02-crm",
  "module": "server.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "scripts": {
    "front": "bun --watch build ./src/crm.ts --outdir ../../public",
    "server": "bun --hot run src/server.ts",
    "build:tailwind": "bunx tailwindcss -c ./front/tailwind.config.cjs -i front/styles.css -o public/output.css --watch"

  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}